import { createTheme as BaseCreateTheme } from "@suid/material/styles";

export const createTheme = BaseCreateTheme({
  palette: {
    primary: {
      main: "rgba(6, 108, 181, 1.0)",
    },
  },
});
